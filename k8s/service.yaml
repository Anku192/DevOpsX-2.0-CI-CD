apiVersion: v1
kind: Service
metadata:
  name: devopsx2-service
spec:
  selector:
    app: devopsx2-app
  type: NodePort
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
      nodePort: 30080